# 会话交接

## 当前目标

用户准备从多模态项目选题转向 **Agentic RL**，并考虑以 `Qwen3.8-27B` 或更小的同类开源模型作为底座。下一次会话应先确定一个可验证、可复现的具体任务环境，再讨论训练实现；目前尚未确定最终领域与数据集。

## 已澄清的核心概念

- **Agent** 是运行时系统：基础模型加上提示词、工具、记忆、循环控制和执行环境。Codex、Claude Code 都属于这类产品形态。
- **Agentic RL** 是训练范式，和 SFT、DPO 一样会更新模型参数；它不是仅靠提示词搭建一个 Agent。
- 三者的主要区别在训练信号：
  - SFT：学习人工演示的目标答案或轨迹。
  - DPO：学习偏好对中的“选中答案优于拒绝答案”。
  - Agentic RL：模型在环境中多步调用工具、接收观察、完成任务后，根据结果奖励更新参数。常用 PPO、GRPO 等算法。
- 常见流程是：`工具格式/基本能力 SFT -> Agentic RL -> 可选 DPO`。DPO 不是 Agentic RL 的必经步骤。

## 关于 Qwen3.8-27B

- [Qwen3.8-27B](https://huggingface.co/Qwen/Qwen3.8-27B) 是 27B 稠密视觉语言模型，原生输入为文本、图像、视频，输出为文本；它不是音频 Omni 模型，也不原生接收点云。
- 因此，原始点云到通信信道预测是专用多模态预测问题，**不是**把 Qwen 做领域适配即可解决的问题。若要用 Qwen，应让它负责推理、规划、读文档、调用仿真和分析结果；点云/信道计算应交给专用模型或外部工具。
- 48GB 显存不适合完整 BF16 训练 27B。推理可考虑量化；训练应优先从 3B/8B 级模型和 LoRA/QLoRA 开始。Agentic RL 还要为多条 rollout 和工具交互留出显存，27B 作为第一轮实验风险较高。

## Agentic RL 项目组成

| 层次 | 作用 | 候选 |
| --- | --- | --- |
| 基础模型 | 生成思考、工具调用和最终答案 | Qwen 3/3.8 的 3B、8B 或 27B 量化模型 |
| Agent 运行时 | 多轮调用工具并把观察结果回传模型 | 自建轻量循环或 `verl-tool` |
| 训练框架 | 收集轨迹、计算奖励、用 GRPO/PPO 更新参数 | `verl` |
| 环境与验证器 | 定义任务、工具返回和客观奖励 | 自建通信仿真环境，或借鉴公开 benchmark |

## 推荐开源项目及定位

- [verl](https://github.com/volcengine/verl)：通用大模型 RL 训练框架，支持 PPO、GRPO 和 Agentic RL；适合作为底层训练器。
- [verl-tool](https://github.com/TIGER-AI-Lab/verl-tool)：在 verl 上实现工具 Agent 的多轮环境交互和训练；最适合作为首个自定义工具 Agentic RL 项目的起点。
- [AgentGym-RL](https://github.com/WooooDyy/AgentGym-RL)：多轮、长程 Agentic RL 研究实现；适合学习环境设计和训练组织。
- [Search-R1](https://github.com/PeterGriffinJin/Search-R1)：检索 Agent 的完整 RL 项目；适合参考“工具调用 + 可验证奖励”的实验结构，不适合直接当通信项目底座。
- [SWE-Gym](https://github.com/SWE-Gym/SWE-Gym)：代码 Agent 的任务环境与评测基准，不是通用训练框架。

这些项目不能混为一类：`verl`/`verl-tool` 是训练基础设施，Search-R1 是特定任务的研究项目，SWE-Gym 是环境与评测。

## 最契合现有背景的可行方向

### 通信仿真工具 Agent

把过去的点云和多径信道经验放到 **可验证工具环境**，而非让语言模型直接拟合信道。

任务形式：模型收到通信场景描述、配置文件、部分测量或文档后，依次调用 `read_docs`、`edit_config`、`run_simulation`、`compute_metrics` 等工具，完成仿真配置、诊断或方案选择。

奖励必须主要来自程序验证，例如：

- 配置是否可运行；
- 输出指标是否满足目标；
- 参数是否在约束范围内；
- 仿真耗时和工具调用次数；
- 最终解释是否引用真实工具输出。

这会是一个真正的 LLM Agentic RL 课题：语言模型学习多步决策和工具使用，物理计算仍由确定性的通信工具完成。首版应选择一个窄任务，例如“给定约束，修复不可运行的链路级仿真配置并达到目标 BER/SNR”，不要一开始做开放式网络规划。

## 建议的实施顺序

1. 先定义一个窄任务、工具接口和确定性验证器；没有可靠奖励时不要先训练。
2. 用小模型搭建能跑通的 Agent 推理循环，记录完整轨迹。
3. 准备少量成功轨迹做 SFT 冷启动，确保模型会正确调用工具。
4. 用 GRPO 在该环境上训练，并保留固定测试集评估任务成功率、成本和泛化。
5. 基线必须包含“同一模型 + 同一工具，但不做 RL”，否则无法证明 Agentic RL 的增益。
6. 小模型实验稳定后，再评估是否值得用 Qwen3.8-27B 做 QLoRA 扩展。

## 下次会话优先确认的问题

1. 最终任务选通信仿真、金融信息工具 Agent、医疗工具 Agent，还是其他领域？
2. 是否已有可执行的通信仿真器、数据集和指标，或需要先搭建一个玩具环境？
3. 可用 GPU 的具体型号、数量和显存是多少？这决定第一版模型和并行策略。

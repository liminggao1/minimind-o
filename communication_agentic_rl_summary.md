点云 xyz + rgb
  -> 材料识别工具
  -> 表面级材料假设与置信度
  -> Agent 决定采用、校准或拒绝该假设
  -> 传统传播模型使用材料信息预测/计算多径
  -> 实测信道反馈验证

   calibrate_materials(
    scene_id="scene_001",
    surface_ids=["wall_17"],
    candidate_materials=["concrete", "glass", "metal"],
    budget=12
)
含义：
- scene_id：当前点云、TX、RX 与测量数据；
- surface_ids：只校准 wall_17，避免一次改全场景；
- candidate_materials：允许尝试的材料类型；
- budget=12：最多运行传统模型 12 次；
- 工具内部：给 wall_17 依次赋予材料配置，运行传统模型并比较预测与实测；
- 返回：每种材料下的误差、推荐配置与置信度。

SFT 框架：
固定的示范轨迹
-> 交叉熵损失
-> 让模型模仿正确工具调用


GRPO / Agentic RL 框架：
模型实时生成轨迹
-> 工具环境返回结果
-> 奖励函数打分
-> GRPO/PPO 更新参数

模型选择：Qwen2.5-VL-7B-Instruct
InternVL3.5-8B
Qwen3.5-9B


# 通信模型校准 Agentic RL 对话总结

## 1. 当前问题与目标

现有传统通信模型的输入为点云 `xyz + rgb`、TX、RX、频率等，输出为预测交互点、各条多径的时延、路径损耗、到达角、发射角等信息。预测结果可能与真实测量存在误差。

目标不是用大模型替换传统模型，也不是让大模型直接预测连续坐标，而是构建一个通信领域 Agent：利用图片语义、传统模型输出与可获得的测量反馈，决定何时、如何调用工具来校准材料假设或其他已暴露的配置，从而降低可观测误差。

## 2. 必须明确的边界

### 传统模型

负责几何和电磁传播计算：

```text
点云 + TX + RX + 频率 + 材料/配置
-> 交互点、多径、时延、路径损耗、AoA/AoD
```

传统模型仍是输出多径信息的核心。

### 大模型 / Agent

负责多步决策，不直接承担精确连续优化：

- 理解图片、场景摘要和通信指标；
- 调用材料识别、传统模型、误差比较与校准工具；
- 选择需要验证的表面、材料候选和计算预算；
- 根据测量反馈接受新配置、继续校准或回退；
- 输出具有依据的诊断和实验结论。

### 数值与几何工具

负责确定性计算和连续优化：

- 由交互点计算路径长度、时延、AoA/AoD；
- 比较预测结果和测量结果；
- 在受限范围内搜索连续参数；
- 修正交互点时，保持其位于对应表面并满足反射顺序、可见性等几何约束。

不应让 LLM 直接生成材料电磁参数或交互点坐标偏移。

## 3. 图片语义如何帮助传统模型

RGB 不能可靠地直接确定无线电材料。视觉相似的墙面可能具有不同介电常数、电导率和表面粗糙度；反之，视觉上不同的表面也可能具有相近的传播特性。

正确方式是把视觉结果当作材料先验，而不是事实：

```json
{
  "surface_id": "wall_17",
  "material_probability": {
    "concrete": 0.55,
    "painted_wall": 0.30,
    "glass": 0.10,
    "metal": 0.05
  }
}
```

即使 `metal` 的视觉先验很低，Agent 仍可在误差较大时将其作为候选材料，通过传统模型和实测反馈检验：

```text
候选材料配置
-> 传统模型预测
-> 与实测 CIR / 时延 / 路损 / AoA 等比较
-> 选择在多个观测上误差更低的材料解释
```

最终判断可理解为：

```text
材料后验 = 视觉先验 + 电磁预测与测量的一致性
```

为了避免单次测量过拟合，候选配置应在多个 TX/RX、频点或独立观测上验证。

图像中的表面必须能与点云表面建立对应关系。需要完成相机与点云的标定、投影或使用共同的 `surface_id`。

## 4. Agent 工具环境

建议将现有能力封装为受限工具：

```python
infer_materials(scene_id)
# 图片或点云渲染图 -> 表面材料概率与置信度

apply_material_profile(scene_id, surface_materials)
# 将候选材料映射为传统模型能使用的真实配置

run_channel_model(scene_id)
# 运行传统模型，返回交互点和多径预测

compute_path_metrics(path)
# 使用确定性几何计算路径长度、时延、AoA/AoD 等

compare_measurement(scene_id)
# 返回预测与实测的误差摘要

calibrate_materials(scene_id, surface_ids, candidate_materials, budget)
# 在受限候选与预算内反复运行传统模型，返回最佳材料配置及误差

validate_candidate(scene_id, candidate_id)
# 使用独立观测验证候选配置

rollback(scene_id)
# 放弃候选结果，回退此前有效配置
```

`calibrate_materials` 不是 LLM。它是一个普通程序：在指定表面和候选材料范围内运行传统模型，并根据测量误差选择较优配置。连续参数的搜索可由贝叶斯优化、CMA-ES、Optuna 等数值优化工具完成。

## 5. Agentic RL 的状态、动作与奖励

一个 episode 是一个场景的完整校准过程。

```text
初始状态：图片、点云摘要、初始材料概率、传统模型预测、可见测量摘要
动作：调用材料识别、运行模型、选择候选、触发校准、验证、接受或回退
观察：各工具返回的材料置信度、误差、成本、物理约束检查结果
终止：配置被接受、回退，或达到预算上限
```

一个 rollout 是 Agent 对同一个场景完成一次从初始状态到终止状态的完整尝试轨迹。GRPO 等算法会针对同一任务生成多条 rollout，并依据奖励推动模型偏向更有效的策略。

奖励建议：

```text
reward =
  预测误差下降
  - 传统模型运行次数成本
  - 无效工具调用惩罚
  - 非法材料/配置惩罚
  - 未通过独立观测验证的惩罚
```

训练时可以把交互点真值、多径真值用于隐藏的奖励计算，但不能直接暴露给 Agent；部署时只能依赖实际可获得的 CIR、RSSI、ToA、AoA 等测量反馈或物理约束。

如果部署时不存在任何测量、真值代理或物理一致性信号，Agent 无法判断校准是否真正更接近真值。

## 6. SFT 与 Agentic RL 的关系

SFT 不只是通信问答。对本项目而言，优先训练“通信 Agent 轨迹”：

```text
图片 + 场景摘要 + 初始多径预测 + 误差摘要
-> 调用工具
-> 接收结果
-> 选择下一步
-> 接受、继续校准或回退
```

建议顺序：

```text

1. Qwen3-8B：
   只接入工具，不训练
   验证它是否能稳定调用材料识别、传统模型和误差评估工具

2. Qwen3-8B + Agent 轨迹 SFT：
   用历史成功校准案例训练
   重点学习工具格式、通信指标含义、接受/回退逻辑
## 重点：
3. Qwen3-8B + Agentic RL：
   在历史场景中隐藏真值
   Agent 仅根据测量反馈行动
   奖励 = 误差下降 - 仿真次数 - 无效动作
   学习在不同反馈状态下选择低成本、高收益动作。

4. 固定测试场景比较：
   比较无 Agent
   数值优化
   SFT Agent
   SFT + RL Agent

5. 流程有效后：
   用同一批轨迹和环境重训 Qwen3.8-27B 的 LoRA，
   或部署 Qwen3.8-27B 作为推理 Agent
```

通信标准、教材和论文可用于 RAG 或知识 SFT，但它们不能替代工具调用轨迹。标准会更新，优先使用 RAG 保持知识可更新。

## 7. 模型候选与建议

| 模型                 | 模态             | 规模 | 适合角色                                                                          |
| -------------------- | ---------------- | ---- | --------------------------------------------------------------------------------- |
| Qwen3.5-9B           | 图像、视频、文本 | 9B   | 当前首选视觉通信 Agent 基座；支持工具调用格式，适合视觉语义和 Agent SFT/RL 探索。 |
| Granite-4.2-8B       | 文本             | 8B   | 当前首选纯文本 Agent 基座；适合先使用结构化材料识别结果和通信工具做 RL。          |
| Qwen3-VL-8B-Instruct | 图像、视频、文本 | 8B   | 成熟的视觉 Agent 对照基线。                                                       |
| InternVL3.5-8B       | 图像、文本       | 8B   | 有 `verl-internvl` 在线 RL 适配，但模型接入和维护成本较高。                       |
| Qwen3.8-27B          | 图像、视频、文本 | 27B  | 适合后续高能力部署和验证，不适合作为单卡 48GB 上首轮视觉 Agentic RL 模型。        |

对于本项目，优先选择 Qwen3.5-9B 做视觉版本，同时可用 Granite-4.2-8B 做只接收结构化材料 JSON 的纯文本版本。两者在相同通信工具环境和测试场景中比较，能够回答“图片语义是否真正降低多径误差”。

## 8. 显存与训练风险

27B 模型的 BF16 权重本身已超过单张 48GB 显存。量化可以降低推理占用，但 Agentic RL 还会占用 rollout 的 KV Cache、图像 token、激活、LoRA 训练状态与奖励/概率缓存。

首轮实验优先使用 8B/9B：

```text
8B/9B 多模态或文本模型
-> 小规模工具调用 SFT
-> 小场景 Agentic RL
-> 固定评测证明奖励设计有效
-> 复用环境和轨迹到 27B 独立微调或部署
```

小模型与 27B 之间不能直接迁移权重；可迁移的是工具接口、训练数据、奖励函数和评测协议。

## 9. 近期应确认的事项

1. 传统模型实际暴露了哪些可被校准的材料或配置接口？
2. 有哪些部署时可获得的测量反馈：CIR、RSSI、ToA、AoA、路径损耗或其他？
3. 图片与点云是否完成了空间标定，能否得到表面级 `surface_id`？
4. 是否具备多个 TX/RX 或频点的独立验证数据，避免材料选择过拟合？
5. 第一版是否只做“单表面材料候选选择”，而不同时引入交互点位置修正？
6. 模型路线是先做 Qwen3.5-9B 视觉 Agent，还是先用 Granite-4.2-8B 验证纯文本工具闭环？

## 10. 核心结论

最有价值的研究问题不是“大模型是否能代替点云多径模型”，而是：

> 当视觉材料先验不可靠、传统通信模型存在误差时，经过 SFT 与 Agentic RL 的多模态 Agent，能否在有限测量和有限仿真预算下，更有效地选择材料假设和校准动作，从而稳定降低多径预测误差？

# Domain Docs

这是一个单项目仓库。

## 开始分析前读取

- 根目录的 `CONTEXT.md`
- `docs/adr/` 下与当前主题相关的架构决策

这些文件不存在时继续工作，不强制提前创建。领域术语和架构决策由后续分析过程中逐步建立。

## 术语约定

问题标题、重构建议、假设和测试名称应优先使用 `CONTEXT.md` 中定义的领域术语。

如果输出与已有架构决策冲突，应明确指出冲突，而不是静默覆盖原决策。
